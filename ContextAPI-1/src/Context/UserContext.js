import React from "react";

// A method for creating Context 
// Context gives provider in this case UserContext which provides props
const UserContext = React.createContext()

// UserContext is just App.jsx where it has every props in global file called UserContext.js





export default UserContext;